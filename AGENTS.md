⚠️ Mandatory: AI agents must read this file before writing or modifying any code.

# AGENTS.md

This file complements the workspace-level Ansiversa-workspace/AGENTS.md (source of truth). Read workspace first.

MANDATORY: After completing each task, update this repo’s AGENTS.md Task Log (newest-first) before marking the task done.

## Scope
- Mini-app repository for 'expense-tracker' within Ansiversa.
- Follow the parent-app contract from workspace AGENTS; do not invent architecture.

## Phase Status
- Freeze phase active: no new features unless explicitly approved.
- Allowed: verification, bug fixes, cleanup, behavior locking, and documentation/process hardening.

## Architecture & Workflow Reminders
- Prefer consistency over speed; match existing naming, spacing, and patterns.
- Keep Astro/Alpine patterns aligned with ecosystem standards (one global store pattern per app, actions via astro:actions, SSR-first behavior).
- Do not refactor or change established patterns without explicit approval.
- If unclear, stop and ask Karthikeyan/Astra before proceeding.

## Where To Look First
- Start with src/, src/actions/, src/stores/, and local docs/ if present.
- Review this repo's existing AGENTS.md Task Log history before making changes.

## Task Log (Recent)
- Keep newest first; include date and short summary.
- 2026-03-22 Initial category bootstrap regression fix: corrected `src/pages/app/expense-tracker.astro` server-side category extraction from `Astro.callAction(actions.listCategories, ...)` to unwrap the nested payload shape consistently on first page load. Root cause from local verification: category tab showed `0 active` until any client-side category create triggered `refreshCategories()`, after which the list appeared. Verification: `npm run typecheck` ✅, `npm run build` ✅.
- 2026-03-22 Drawer-standard alignment for app forms: updated `src/pages/app/expense-tracker.astro`, `src/modules/expense-tracker/store.ts`, and `src/modules/app/drawerStore.ts` so account-create and category-create now follow the same side-drawer form pattern already used by transactions. Inline create forms were removed from the tab panels; `Create account` and `Create category` now open dedicated drawers, preserve validation/error messaging in the drawer footer, and close automatically on successful save. Verification: `npm run typecheck` ✅, `npm run build` ✅.
- 2026-03-22 Compact table layout update for tabbed CRUD sections: converted `/app/expense-tracker` `Accounts`, `Categories`, and `Transactions` panels in `src/pages/app/expense-tracker.astro` from stacked record cards/month groups to shared `AvTable`-based tables so each record stays on one row and vertical scroll is reduced. Preserved existing create/archive/edit/delete flows, empty states, quick transaction type updates, and tab behavior; added compact table-specific cell/input styling for readability. Verification: `npm run typecheck` ✅, `npm run build` ✅.
- 2026-03-22 `/app/expense-tracker` tabbed workspace layout approved and implemented: reorganized the long vertical single-page flow in `src/pages/app/expense-tracker.astro` into local Alpine tabs (`Overview`, `Accounts`, `Categories`, `Transactions`) while preserving the existing SSR-first data bootstrapping, global store/actions workflow, account/category/transaction CRUD behavior, and transaction drawer flows. Added a lightweight local tab switcher card plus responsive tab styling; overview now holds summaries, filters, insights, and quick actions, while CRUD sections render one at a time for calmer scanning. Verification: `npm run typecheck` ✅, `npm run build` ✅.
- 2026-03-22 Action response unwrap fix after live local verification: corrected `src/modules/expense-tracker/store.ts` `unwrapResult()` to handle the actual `astro:actions` client payload shape (`{ data: { success, data } }` / `{ success, data }`) instead of only unwrapping one level. Root cause from browser verification: account creation returned success (`success === 'Account created.'`) but `$store.expenseTracker.accounts` stayed empty because `data.account` and refreshed `data.items` were being read from the wrong level. This fix restores immediate account/category/transaction state updates after action calls without manual reload. Verification: `npm run typecheck` ✅, `npm run build` ✅.
- 2026-03-22 Chart key crash fix after local tester console report: fixed `src/components/charts/BarListChart.astro` to stop keying Alpine loops by non-unique `item.label`, using a stable fallback key instead, and updated `src/modules/expense-tracker/store.ts` so `categoryChartItems` carries a canonical `key` derived from grouped category identity (`categoryId` or `uncategorized`). This prevents duplicate-key Alpine crashes in the category spending chart that could break later UI reactivity on `/app/expense-tracker`, including account-list updates after create. Verification: `npm run typecheck` ✅, `npm run build` ✅.
- 2026-03-22 Verification only for tester report "created account is not displayed without page reload": confirmed the existing refresh fix is still present in `src/modules/expense-tracker/store.ts` (`createAccount` prepends the returned account and then refreshes via `listAccounts`), verified local repo `HEAD` matches `origin/main` at `16a5254`, and re-ran `npm run typecheck` ✅ plus `npm run build` ✅. Also confirmed the live alias `https://expense-tracker.ansiversa.com` currently resolves to Vercel deployment `dpl_9BBzR2D387bXrPGbrDBm2ePgqLXC` created on 2026-03-21. No code changes required from this verification pass.
- 2026-03-21 Issue 5 transaction-create validation fix: hardened `src/modules/expense-tracker/store.ts` so expense/income transactions now require a category before submit, and strengthened `src/actions/index.ts` so the shared transaction reference validator rejects expense/income create/update requests without a category with a clear message (`Please select a category.`). This closes the broken no-category submission path that left the user in a stuck `Saving...` flow. Verification: `npm run typecheck` ✅, `npm run build` ✅.
- 2026-03-21 Issue 3 category-create refresh fix: hardened `src/modules/expense-tracker/store.ts` so successful category creation now refreshes the canonical category list from `listCategories` after the immediate local insert, ensuring new categories appear in the Categories UI without a manual reload. Verification: `npm run typecheck` ✅, `npm run build` ✅.
- 2026-03-21 Issue 2 account-management visibility fix: added a visible per-account `Archive` action on the Accounts cards in `src/pages/app/expense-tracker.astro` and wired `src/modules/expense-tracker/store.ts` to the existing `archiveAccount` action with confirmation plus canonical account-list refresh. This preserves the repo’s historical-truth archive model while letting testers remove duplicate or mistaken accounts from normal UI lists. Verification: `npm run typecheck` ✅, `npm run build` ✅.
- 2026-03-21 Issue 1 account-create refresh fix: hardened `src/modules/expense-tracker/store.ts` so successful account creation now refreshes the canonical account list from `listAccounts` after the immediate local prepend, ensuring the new account appears in the Accounts UI without a manual reload. Verification: `npm run typecheck` ✅, `npm run build` ✅.
- 2026-03-18 Components lock sync: upgraded `@ansiversa/components` to `^0.0.169` and refreshed the lockfile for the pre-launch ecosystem lock. Verification: `npm run typecheck` ✅, `npm run build` ✅.
- 2026-03-14 Bumped `@ansiversa/components` to `^0.0.168` after the published `AppLogoExpenseTracker` release so Expense Tracker consumes the shared logo-capable package version; verified install resolution and `npm run build` against the published package.
- 2026-03-14 Final historical integrity fix pass after Astra verification: aligned archived-category handling with historical-truth rules by loading full category data for home/app reporting while keeping normal category-management and new-entry selectors filtered to active compatible categories; hardened `src/actions/index.ts` so transaction create/update now enforce type-to-category compatibility (`expense`→expense category, `income`→income category, `transfer`→no category in V1) and category create/update now validate `parentCategoryId` ownership/existence plus prevent self-parenting; refreshed store/UI/docs in `src/modules/expense-tracker/store.ts`, `src/pages/app/expense-tracker.astro`, `src/pages/index.astro`, `src/pages/help.astro`, and `README.md` to preserve historical category truth without exposing archived categories in normal selection flows. Verified with `npm run typecheck` and `npm run build`.
- 2026-03-14 Final freeze fix pass after Astra verification: removed the hidden 200-transaction correctness cap by bootstrapping `/app/expense-tracker` from full server-side account/transaction queries instead of a paginated first-page action slice, while keeping account-management UI filtered to active accounts only; made the archived-account rule explicit and consistent across backend and app calculations by keeping archived accounts in historical/current totals but hiding them from normal account-management lists and selectors; updated `src/actions/index.ts` so create/update/archive flows for accounts, categories, and transactions now return canonical persisted rows fetched from the database after writes instead of merged in-memory guesses. Verified with `npm run typecheck` and `npm run build`.
- 2026-03-14 Freeze fix pass after Astra verification: hardened `src/actions/index.ts` so transaction create/update now validates ownership of `accountId`, `categoryId`, and `transferAccountId`, enforces transfer source/destination rules, and returns clear server-side validation errors; corrected `listTransactions.total` to report full filtered count; locked V1 currency behavior to a single effective user currency with action-level enforcement and mixed-currency trust warnings; updated summary semantics so current balance now includes account starting balances while period cards are labeled as net flow; refreshed `src/pages/app/expense-tracker.astro`, `src/pages/index.astro`, `src/pages/help.astro`, `src/dashboard/summary.schema.ts`, `src/modules/expense-tracker/store.ts`, and `README.md` to match the frozen app contract. Verified with `npm run typecheck` and `npm run build`.
- 2026-03-14 Public home-page landing refactor: rebuilt `src/pages/index.astro` so anonymous visitors now see a Resume-Builder-style static landing flow (hero, descriptive summary, feature highlights, `Why Expense Tracker`, `How it works`, and final CTA) instead of dashboard metrics/empty-state placeholders. Kept the existing data-backed summary/recent-activity rendering only for authenticated users. Verified with `npm run typecheck` and `npm run build`.
- 2026-03-13 Home-page empty-state fix for local seeded data: diagnosed that the landing page `/` was rendering empty despite seeded remote data because its data path depended on auth/action context that did not reliably populate on the public home route in local DEV. Added configurable DEV bypass identity support in `src/middleware.ts` (`DEV_AUTH_BYPASS_USER_ID`, `DEV_AUTH_BYPASS_EMAIL`, `DEV_AUTH_BYPASS_NAME`), documented it in `README.md`, set local `.env` to the demo user `01e5cef7-b18d-4616-999c-454175356c24`, forced `src/pages/index.astro` to `prerender = false`, and switched the home page to resolve an effective user id (locals user or DEV bypass user) and query `astro:db` directly for categories/transactions before computing the summary. Verified with `npm run typecheck`, `npm run build`, and local HTML probe showing the empty markers removed and `Top expense category: Housing`.
- 2026-03-13 Expense-density SQL follow-up for demo user `01e5cef7-b18d-4616-999c-454175356c24`: verified existing remote data already contained 29 expense rows, then inserted 12 additional expense-only transactions directly via remote libSQL SQL/API across recent March dates to make expense activity denser in the feed and filters. New totals after insert: `expense count=41`, `expense total=28827`; latest added rows include groceries, dining, fuel, utilities, shopping, health, and family-support entries on 2026-03-04 through 2026-03-13.
- 2026-03-13 Mini-app bar title/menu correction: after upgrading shared components, the bar still showed slug text because `expense-tracker` is not present in the shared mini-app registry. Replaced the repo `AppShell` mini-app-bar portion with a local explicit bar using shared navbar/footer primitives so the title renders as `Expense Tracker` and the 3-dot menu uses the intended local links (`Home`, `Expense Tracker`, `Help`). Removed the temporary `src/app.meta.ts` approach. Verified with `npm run typecheck` and `npm run build`.
- 2026-03-13 Mini-app navbar restore: diagnosed missing navbar as a shared-package/version gap plus layout wiring gap. Added `src/app.meta.ts`, updated `src/layouts/AppShell.astro` to pass `miniAppKey="expense-tracker"` and app links into `WebLayout`, and upgraded `@ansiversa/components` from `^0.0.75` to `^0.0.167` so the shared mini-app navbar contract exists in this repo. Verified with `npm run typecheck` and `npm run build`.
- 2026-03-13 Remote demo data seeded for user `01e5cef7-b18d-4616-999c-454175356c24` using the repo’s remote libSQL connection from `.env`: inserted a realistic UAE-based dataset directly into `Accounts`, `Categories`, and `Transactions` (4 accounts, 13 categories, 39 dated transactions across Dec 2025 to Mar 2026) with salary, freelance income, savings transfers, rent, groceries, utilities, subscriptions, shopping, fuel, health, and family-support activity; verified remote totals (`income=93050`, `expense=27075`, `transfers=20500`) plus recent transaction and category breakdown queries.
- 2026-03-13 Localhost redirect fix aligned to `resume-builder`: compared auth boundary behavior and updated `src/middleware.ts` so `/` and `/help` stay public locally, protected routes use `http://localhost:2000` as the DEV parent fallback instead of `https://ansiversa.com`, and static-path handling matches the proven pattern more closely. Updated `src/pages/index.astro` to avoid auth-required action calls when unauthenticated, documented the behavior in `README.md`, and verified with `npm run typecheck` plus `npm run build`.
- 2026-03-12 Local dev startup hardening follow-up: reproduced port-collision behavior where another server on `4321` caused Astro to auto-shift ports; updated `package.json` dev script to `astro dev --remote --port 4321 --strictPort` so collisions fail fast with a clear error, and updated `README.md` with strict-port guidance.
- 2026-03-12 Local dev auth unblock: diagnosed localhost redirect confusion (`4321` can point to another running app and unauthenticated middleware redirects to parent login by design); added opt-in DEV-only middleware bypass via `DEV_AUTH_BYPASS=true` in `src/middleware.ts`, added env typing in `src/env.d.ts`, documented usage in `README.md`, and verified with `npm run typecheck` plus local HEAD check (`200 OK` on `http://localhost:4321/` when bypass is enabled).
- 2026-03-10 Slice 7 smart quick actions and fast entry flow: added store-level quick entry derivations and handlers in `src/modules/expense-tracker/store.ts` (`lastExpenseTransaction`, `lastIncomeTransaction`, `recentReusableTransactions`, preset-aware `openCreateTransaction`, `openQuickAdd`, repeat/reuse helpers, and quick common category derivations); updated `/app/expense-tracker` with compact quick action row, repeat action states, recent reusable transactions section, and create-drawer quick-entry affordances including common category quick picks; added `docs/verification/expense-tracker-slice-7-checklist.md`; validated with `npm run typecheck` and `npm run build`.
- 2026-03-10 Slice 6 smart financial insights: added deterministic local insight derivations in `src/modules/expense-tracker/store.ts` (`smartInsights`, `quickInsightTake`, period month-over-month comparison helper) using existing filtered totals/category/recent/monthly data; added Smart insights panel on `/app/expense-tracker` with concise 3–5 insights, tone badges (`info`, `watch`, `positive`), and empty-state guidance; added `docs/verification/expense-tracker-slice-6-checklist.md`; validated with `npm run typecheck` and `npm run build`.
- 2026-03-10 Slice 5 lightweight reusable charts: added local generic chart components (`BarListChart`, `MiniTrendChart`, `ComparisonStatChart`) under `src/components/charts/` with empty-state support and no external chart packages; wired `/app/expense-tracker` to period-aware chart data from Alpine store derived fields (`categoryChartItems`, `monthlyTrendItems`, `incomeVsExpenseComparison`); added `docs/verification/expense-tracker-slice-5-checklist.md`; validated with `npm run typecheck` and `npm run build`. Charts are implemented as local generic components intended for possible future promotion to the shared Ansiversa component library after validation.
- 2026-03-10 Slice 4 date filters and period insights: added period presets (all time, this month, last month, this year, last 30 days) plus custom start/end range validation on `/app/expense-tracker`; made summary cards, category insights, largest recent expenses, and monthly transaction groups period-aware with clear filtered empty states; added `docs/verification/expense-tracker-slice-4-checklist.md`; validated with `npm run typecheck` and `npm run build`.
- 2026-03-10 Slice 3 category insights and monthly view: enhanced `/app/expense-tracker` with current-month summary metrics, category expense insights with top category highlight, monthly-grouped transaction sections, and largest recent expense callout; added Slice 3 verification checklist at `docs/verification/expense-tracker-slice-3-checklist.md`; validated with `npm run typecheck` and `npm run build`.
- 2026-03-10 Slice 2 premium home and summary polish: rebuilt `src/pages/index.astro` with premium two-column hero, summary panel/strip, value-pillar section, and recent-transactions section (with empty state CTA); strengthened `/app/expense-tracker` top summary cards to use live Alpine totals and transaction count; added Slice 2 verification checklist at `docs/verification/expense-tracker-slice-2-checklist.md`; validated with `npm run typecheck` and `npm run build`.
- 2026-03-08 Follow-up deployment for Slice 1 verification: pushed commits `fe9143c` and `291c362` to `origin/main`, deployed production via Vercel (`dpl_2WWKDwHwGz5HiqYWsuMY16jN1T9y`), and confirmed deployment status `Ready` at `https://expense-tracker-57zqor6zu-srilakshmi-tailors-team.vercel.app` (alias `https://expense-tracker.ansiversa.com`). Manual browser CRUD verification remains pending.
- 2026-03-08 Slice 1 completion from existing baseline: reused existing middleware/session boundary, DB tables (`Accounts`, `Categories`, `Transactions`), and action handlers; added missing user app route (`/app/expense-tracker`) with summary cards, account/category create flows, transaction list/edit/delete, and drawer-based create/edit transaction UX via Alpine stores. Added summary contract baseline (`src/dashboard/summary.schema.ts`) and verification checklist (`docs/verification/expense-tracker-slice-1-checklist.md`), then validated with repo commands (`npm run typecheck`, `npm run build`).
- 2026-02-09 Added repo-level AGENTS.md enforcement contract (workspace reference + mandatory task-log update rule).
- 2026-02-09 Initialized repo AGENTS baseline for single-repo Codex/AI safety.
